* This line won't display anything, occurs for for/if

Map Values = initialValues // Initialization, 1st line

* for ( var current = 2; var <= problem.length; current++):
    * if (currentLine != blockStartLine) // If not in if/else block
       updateValues(currentLine)
    else // reached if/else block
        while(!nextBool) // while next evaluated bool is false
            currentLine += numberOfStatements + 1
        updateValues(currentLine)
        currentLine = blockEndLine + 1 // crossout of everything else would occur here
printStatement
